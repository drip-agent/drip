# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | scope | Project name | DRIP ($DRIP) | Crypto-native ("dripping alpha"), water theme matches palette, short/memeable, PumpFun-friendly ticker | No |
| D002 | M001 | scope | Domain | drip.surf | "Surf" = ocean/water (palette match) + surfing data (agent function). agent.drip.surf for agent subdomain | No |
| D003 | M001 | convention | Color palette | Icy Aqua #bdfffd, Soft Cyan #9ffff5, Aquamarine #7cffc4, Ocean Mist #6abea7, Blue Slate #5e6973 | User-provided palette with named colors | No |
| D004 | M001 | library | Animation stack | GSAP + Framer Motion | GSAP for scroll-driven/particle/complex timelines, Framer Motion for React UI transitions. Industry standard combo for 2026 | Yes — if performance issues arise |
| D005 | M001 | convention | Design direction | Dark futuristic with aqua glow | User: "sangat canggih dan futuristik modern dan juga beranimasi". Dark backgrounds, glowing aqua accents, glassmorphism | No |
| D006 | M001 | convention | Agent voice | Cool & mysterious | Drops value quietly, doesn't try hard, lets quality speak. Ocean mist energy. Never hype. | No |
| D007 | M001 | scope | Target audience | Crypto/Solana degens | Traders, memecoin hunters, alpha seekers who want edge and entertainment | Yes — if expanding beyond crypto |
| D008 | M002 | scope | First agent skill | People & company research (StableEnrich) | Immediately tangible, high-value alpha, 30 routes available via AgentCash | Yes — could add skills in any order |
| D009 | M003 | scope | Token platform | PumpFun with Tokenized Agents | PumpFun is dominant launchpad (73%+ market share). Tokenized Agents feature (March 13, 2026) enables revenue-to-buyback loop | No |
| D010 | M001 | scope | Marketing channel | X/Twitter build-in-public | User documents what they build on X. X is growth engine, not product. Agent lives on web only. | No |
| D011 | M002 | arch | Agent UX model | Chat + discovery feed | Chat for interactive research, public feed for autonomous discoveries. Two value surfaces. | Yes — if one proves unnecessary |
| D012 | M001 | library | Frontend framework | Next.js 15 (App Router) | SSR, API routes for M002 backend, Vercel deployment, TypeScript, industry standard | Yes — if requirements change |
| D013 | M001/S01 | convention | Design tokens via Tailwind v4 @theme | CSS-first @theme in globals.css, not tailwind.config.ts | Tailwind v4 auto-generates utilities from @theme CSS custom properties. Roadmap boundary map says "tailwind.config.ts color values" — superseded by v4 convention. Same tokens, cleaner delivery. | No |
| D014 | M001/S01 | convention | Typography stack | Space Grotesk (headings) + Inter (body) + JetBrains Mono (code) | Space Grotesk is top-cited for futuristic/tech/crypto. Inter is body text standard. JetBrains Mono for code/data. All Google Fonts, loadable via next/font/google. | Yes — if brand direction shifts |
| D015 | M001/S01 | library | Next.js version | Next.js 16.1.6 (latest) | D012 says "Next.js 15" but create-next-app@latest installs 16.x in March 2026. Same App Router, same API surface. Tailwind v4 integration identical. No breaking changes affect S01–S05. | Yes — pin if stability issues arise |
| D016 | M001/S02 | library | Component variant stack | CVA (stable v0.7.x) + clsx + tailwind-merge | Industry standard cn() pattern (shadcn/ui popularized). Type-safe variants via CVA VariantProps, conflict-free className overrides via twMerge. Stable CVA over beta — proven, simpler than defineConfig. | Yes — if migrating to a component library |
| D017 | M001/S02 | convention | Client/server component boundary | Server components by default; 'use client' only on Button, Input, NavBar | Minimize client bundle. Only components with event handlers (onClick, onChange, scroll listener) need client directive. Keeps 7/10 components as server components. | Yes — if more interactivity needed |
| D018 | M001/S02 | convention | Component directory structure | UI primitives in components/ui/, layout in components/layout/ | Separates atomic design primitives (Button, Badge, Card) from structural layout (Container, Section, NavBar, Footer). Matches shadcn/ui conventions. | No |
| D019 | M001/S02 | convention | Showcase page pattern | Data-driven const arrays mapped to component grids | /brand and /design both use this pattern. Const arrays of variants/sizes rendered via .map(). Easy to extend, consistent across showcase pages. | No |
| D020 | M001/S03 | convention | Animation component directory | components/animation/ | Extends D018 pattern: ui/ for primitives, layout/ for structure, animation/ for motion components. Keeps animation concerns (all 'use client', all browser-API-dependent) separate from static UI components. | No |
| D021 | M001/S03 | arch | Particle rendering engine | Canvas 2D (not WebGL) | Simpler API, broader mobile support (especially Safari), sufficient for floating particle effects. requestAnimationFrame for render loop — self-contained, independently optimizable, no GSAP ticker dependency. | Yes — if visual complexity demands WebGL |
| D022 | M001/S04 | convention | Landing page section composition | 5 sections: Hero + Features + How It Works + Agent Preview + CTA | Hero with ParticleField atmosphere, features with animated card grid, step-by-step pipeline, mock terminal preview, final CTA. Each section has anchor ID matching NavBar links for smooth scroll. | Yes — sections can be added/reordered |
| D023 | M001/S04 | convention | ScrollReveal usage limit | Max 5-6 ScrollReveal instances per page, FadeInStagger for groups | ScrollReveal registers individual GSAP ScrollTrigger observers — too many causes performance overhead. Card grids use single FadeInStagger (one observer) instead. | No |
| D024 | M001/S04 | convention | OG image generation | Next.js file-convention at app/opengraph-image.tsx, twitter-image re-exports | Zero-dependency inline JSX styling, single source of truth. twitter-image.tsx re-exports opengraph-image.tsx rather than duplicating. | Yes — if custom per-page OG images needed |
| D025 | M001/S04 | convention | Hero viewport unit | min-h-dvh (not min-h-screen) | dvh (dynamic viewport height) accounts for iOS Safari address bar. min-h-screen includes hidden browser chrome on mobile, causing oversized hero. | No |
| D026 | M001/S05 | convention | OG font loading for Satori | IE-compat User-Agent to get TTF from Google Fonts API | Satori rejects woff2 (signature error). Google Fonts serves TTF when given an IE11 User-Agent. Font utility in lib/og-fonts.ts uses IE UA to get truetype format, with fallback to any format match. All ImageResponse routes share this utility. | Yes — if bundling local .ttf instead |
| D027 | M002 | arch | Subdomain routing strategy | Single codebase with Next.js middleware rewriting agent.drip.surf/* to /agent/* routes | One deployment, one build, shared design system. Middleware excludes /_next, /api, static files. Standard Vercel pattern (Platforms Starter Kit). Separate project would duplicate the design system and complicate deploys. | No |
| D028 | M002 | library | LLM provider | Claude (Anthropic) via @ai-sdk/anthropic | Better at maintaining consistent voice/personality across conversations — critical for the "cool & mysterious" agent character (D006). Vercel AI SDK's streamText works identically with any provider, so switching is low-cost if needed. | Yes — if Claude availability or pricing becomes a problem |
| D029 | M002 | arch | Feed persistence | Vercel KV (Upstash Redis) | Lightest sufficient storage. Zero-config with Vercel, generous free tier (256MB, 30K cmds/day), simple get/set/list API. No schema migrations. Vercel Postgres is overkill for key-value feed entries. Filesystem is ephemeral on Vercel (data lost on redeploy). | Yes — if data model becomes relational |
| D030 | M002 | arch | AgentCash integration mode | Direct x402 HTTP via @x402/fetch SDK (not MCP mode) | MCP mode is designed for CLI agents with a local MCP server. Web backend needs direct HTTP calls with payment wrapping. @x402/fetch handles the full 402→sign→retry flow by wrapping native fetch. Battle-tested, maintained by Coinbase ecosystem. | No |
| D031 | M002 | arch | Session memory model | In-memory per API request (no persistence) | Simplest correct option for v1 without auth (R016 deferred). useChat manages message history client-side within a browser session. No server-side conversation state. Upgrading to KV-backed sessions later is additive, not breaking. | Yes — when R016/R018 are implemented |
| D032 | M002 | library | LLM orchestration framework | Vercel AI SDK (ai + @ai-sdk/react + @ai-sdk/anthropic) | streamText + useChat + tool() maps perfectly to the skill architecture. Purpose-built for streaming chat with server-side tool execution. 11K+ installs. Handles stream protocol, message conversion, multi-step tool calls. Don't hand-roll streaming. | Yes — if AI SDK has compatibility issues with Next.js 16 |
| D033 | M002/S01 | convention | Next.js 16 proxy file convention | `proxy.ts` with `export function proxy()` instead of `middleware.ts` / `middleware()` | Next.js 16.1.6 deprecates the `middleware` file convention in favor of `proxy`. Same API surface (NextRequest, NextResponse, config.matcher), just renamed. Build warns if using old convention. | No |
| D034 | M002/S01 | library | x402 client composition | `toClientEvmSigner(account, publicClient)` + `registerExactEvmScheme()` | x402 v2 `ExactEvmScheme` expects `ClientEvmSigner` with `readContract`, not a bare viem account. `toClientEvmSigner` bridges a `privateKeyToAccount` + `publicClient` into the full signer interface. | No |
| D035 | M002/S01 | library | @x402/evm explicit dependency | Added `@x402/evm` as explicit dep (not transitive from `@x402/fetch`) | `@x402/fetch` only pulls in `@x402/core`, not `@x402/evm`. The EVM scheme for payment signing needs explicit installation. | No |
| D036 | M002/S01 | library | LLM provider: OpenRouter | `@openrouter/ai-sdk-provider` via `createOpenRouter()` instead of `@ai-sdk/anthropic` (supersedes D028) | User chose OpenRouter for model flexibility — access to Claude and other models via single API key. AI SDK provider interface identical; switching back is one-line change in route.ts. | Yes — if reverting to direct Anthropic |
| D037 | M002/S01 | convention | AI SDK v6 API surface | `sendMessage()` + `status` + `DefaultChatTransport` + `inputSchema` + `stepCountIs()` + `toUIMessageStreamResponse()` | AI SDK v6 broke: `input`/`handleInputChange`/`handleSubmit` deprecated in useChat, `parameters` → `inputSchema` in tool(), `maxSteps` → `stopWhen: stepCountIs(n)`, `toDataStreamResponse` → `toUIMessageStreamResponse`, tool part state `result` → `output-available`. | No |
| D038 | M003 | arch | Revenue model | Pay-per-query via PumpFun invoices | Each chat query generates a PumpFun SDK invoice. User pays USDC on Solana, payment verified before agent responds. Matches AgentCash's per-request pricing and the SDK's invoice-per-action design. No free tier — keeps revenue loop simple and honest. | Yes — could add free tier or subscription later |
| D039 | M003 | arch | Payment currency | USDC on Solana (start single-currency) | Stable pricing for both users and revenue accounting. SOL support is additive later — SDK accepts both. Avoids price volatility in revenue calculations. | Yes — add SOL as second option |
| D040 | M003 | arch | Slice ordering rationale | Payment integration (S01) before token display (S02) | Prove the 2-day-old SDK and Solana dependency chain work before building display UI that depends on revenue data. If SDK fails, fallback is raw `@solana/web3.js` — need to know early. | No |
| D041 | M003/S01 | convention | Payment verification flow | Server builds invoice → client signs → client sends tx → server verifies on-chain → chat proceeds | SDK builds instructions server-side (needs agent keypair), user signs client-side (browser wallet), verification is server-side (RPC check). The `x-payment-invoice` header carries the verified invoice ID to the chat route. Invoice consumed on use to prevent replay. | No |
| D042 | M003/S01 | convention | Dual wallet display in agent nav | Both `WalletBadge` (EVM/x402) and `ConnectButton` + `RevenueBadge` (Solana) coexist in agent nav | Two independent wallet ecosystems: agent's EVM wallet pays for AgentCash APIs (server-side), user's Solana wallet pays the agent for queries (client-side). Both are visible in nav. Neither replaces the other. | No |
| D043 | M003/S01 | convention | Revenue KV schema | `revenue:total_earned` (string), `revenue:query_count` (number), `invoice:{id}` (object with verified/consumed/timestamp) | Simple flat KV keys following existing `feed:*` naming pattern. Invoice records prevent replay attacks (consumed flag). Revenue endpoint reads these keys directly — no aggregation needed. | Yes — if revenue data model grows |
| D044 | M003/S01 | arch | PumpAgent singleton — no private key for payment flow | PumpAgent constructor takes `(mint, environment, connection)` — not a Keypair. SDK handles payment acceptance/verification without agent signing. Private key only needed for admin ops (withdraw, distribute). Singleton exposes `{ agent, connection, tokenMint }`. | Task plan said use `Keypair.fromSecretKey()` but SDK API doesn't require it for `buildAcceptPaymentInstructions` or `validateInvoicePayment`. Verified via type definitions. Keeps the singleton simpler and avoids requiring the private key at payment time. | No |
| D045 | M003/S01 | arch | Dynamic headers via ref on DefaultChatTransport | `invoiceRef.current` set before `sendMessage()`, transport reads via `Resolvable<Record<string, string>>` header function | Avoids recreating transport per message. The `headers` option on `DefaultChatTransport` accepts a function — ref is read at request time. Cleared via setTimeout after send. Clean separation of payment flow from chat transport. | No |
| D046 | M003/S01 | convention | Suggestions set input instead of auto-sending | Clicking a suggestion fills the input field; user must click Send (triggering payment flow) | Forces every message through the payment gate. Auto-send would bypass the wallet connection check and payment flow. Better UX for paid queries — user sees what they're paying for before committing. | Yes — if free tier added |
