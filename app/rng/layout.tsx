export const metadata = {
  title: "RNG — DRIP",
  description: "Payment-gated random number generator. Pay 0.1 SOL to generate a random number 0-1000.",
};

export default function RngLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
